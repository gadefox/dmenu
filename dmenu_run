#!/bin/rc

if (~ $* ()) {
  app = `{ dmenu_path | dmenu -p 'Launch executable' }
  ~ $app () && exit 0
  $app &
  exit 0
}

app = `{ dmenu_path | dmenu -p 'Launch terminal' }
~ $app () && exit 0
st -e $app &